// The page-extractor card is a `card` document whose behavior module
// (./dist/card.js) the shared card shell loads; the JSON cards it mints load
// ./json-card.js the same way. This package registers nothing; it exists only
// to publish those modules.
export const plugins = [];
