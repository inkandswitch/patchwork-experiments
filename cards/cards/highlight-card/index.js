// The Highlight card: draws a blue glow around every mounted <patchwork-view>
// whose document is in the shared `highlight` channel (owned by the Selection
// card). Its behavior module (./card.js) is loaded by the shared card shell.
// This package registers nothing — it exists only to publish that module.
export const plugins = [];
