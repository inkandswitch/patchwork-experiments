# Place Finder

A card that answers [searches](../core) with real-world places: it looks each
query up on OpenStreetMap and adds the matches, biased toward places already
nearby.

Each result is a small place card of its own:

- a name
- a coordinate (latitude and longitude)
- a rough kind, like "city" or "park"
