// The Timer card is a `card` document whose behavior module (./card.js) the
// shared card shell loads. The countdown widget it mints tokens for is the
// `timer` tool registered by the Stickers card package. This package registers
// nothing; it exists only to publish that module.
export const plugins = [];
