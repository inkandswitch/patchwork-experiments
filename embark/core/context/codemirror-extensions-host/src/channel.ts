// The canonical definition lives in the core package root's hand-written
// channels/codemirror.js (with `definedBy`/`spec` attribution) so publisher
// cards can import it by the core package's automerge url; the bundled host
// code re-exports it from there.
export { CodemirrorExtensions } from "../../../channels/codemirror.js";
