# Optional: persist automerge docs to this directory on disk (dev server only)
# VITE_STORAGE_DIR=./bullets-data

# Optional: WebSocket URL of an external automerge sync server
# VITE_SYNC_URL=ws://localhost:3030
